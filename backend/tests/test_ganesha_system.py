"""
GANESHA System Integration Tests
Tests the complete GANESHA intelligence layer
"""

import pytest
import asyncio
from motor.motor_asyncio import AsyncIOMotorClient
import sys
import os

# Add parent directory to path
sys.path.insert(, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from agents.ganesha_agents import (\n    SARASWATI, NARADA, RIHASPATI, CHITRAGUPTA, AGASTYA,\n    GaneshaSubAgentOrchestrator\n)\nfrom agents.command_processor import CommandProcessor, ConversationContextManager\nfrom agents.task_router import TaskRouter\n\n# Test configuration\nTEST_MONGO_URL = os.environ.get('MONGO_URL', 'mongodb://localhost:')\ntest_db = None\n\n\n@pytest.fixture(scope=\"module\")\ndef event_loop():\n    \"\"\"Create event loop for async tests\"\"\"\n    loop = asyncio.get_event_loop_policy().new_event_loop()\n    yield loop\n    loop.close()\n\n\n@pytest.fixture(scope=\"module\")\nasync def db():\n    \"\"\"Setup test database\"\"\"\n    global test_db\n    client = AsyncIOMotorClient(TEST_MONGO_URL)\n    test_db = client.kailash_test\n    \n    # Clean test database\n    await test_db.ceo_commands.delete_many({})\n    await test_db.tasks.delete_many({})\n    await test_db.agent_activity.delete_many({})\n    await test_db.knowledge_base.delete_many({})\n    await test_db.inter_agent_messages.delete_many({})\n    await test_db.ceo_decisions.delete_many({})\n    await test_db.conversation_contexts.delete_many({})\n    \n    # Insert test knowledge\n    await test_db.knowledge_base.insert_many([\n        {\n            \"title\": \"URJAA Platform\",\n            \"content\": \"URJAA is an EV charging infrastructure platform.\",\n            \"tags\": [\"urjaa\", \"ev\", \"charging\"]\n        },\n        {\n            \"title\": \"Go4Garage\",\n            \"content\": \"Go4Garage is a garage management software platform.\",\n            \"tags\": [\"go4garage\", \"garage\"]\n        }\n    ])\n    \n    yield test_db\n    \n    # Cleanup\n    client.close()\n\n\nclass TestSARASWATI:\n    \"\"\"Test SARASWATI knowledge manager\"\"\"\n    \n    @pytest.mark.asyncio\n    async def test_knowledge_retrieval_found(self, db):\n        \"\"\"Test retrieving existing knowledge\"\"\"\n        agent = SARASWATI(db)\n        result = await agent.process(\"What is URJAA?\", {})\n        \n        assert result['success'] == True\n        assert result['agent'] == 'SARASWATI'\n        assert 'URJAA' in result['result'] or 'charging' in result['result'].lower()\n        assert result['sources'] > \n    \n    @pytest.mark.asyncio\n    async def test_knowledge_retrieval_not_found(self, db):\n        \"\"\"Test handling unknown topic\"\"\"\n        agent = SARASWATI(db)\n        result = await agent.process(\"Tell me about quantum computing\", {})\n        \n        assert result['success'] == False\n        assert result['agent'] == 'SARASWATI'\n        assert 'knowledge base' in result['result'].lower()\n\n\nclass TestNARADA:\n    \"\"\"Test NARADA communication coordinator\"\"\"\n    \n    @pytest.mark.asyncio\n    async def test_message_routing_finance(self, db):\n        \"\"\"Test routing to finance department\"\"\"\n        agent = NARADA(db)\n        result = await agent.process(\n            \"inance team, review Q4 budget\",\n            {\"command_id\": \"test-3\", \"priority\": \"P\"}\n        )\n        \n        assert result['success'] == True\n        assert result['agent'] == 'NARADA'\n        assert 'LAKSHMI' in result['target_departments']\n        assert result['messages_created'] > \n    \n    @pytest.mark.asyncio\n    async def test_message_routing_multi_department(self, db):\n        \"\"\"Test routing to multiple departments\"\"\"\n        agent = NARADA(db)\n        result = await agent.process(\n            \"Tech and finance teams, collaborate on cost optimization\",\n            {\"command_id\": \"test-4\", \"priority\": \"P\"}\n        )\n        \n        assert result['success'] == True\n        assert len(result['target_departments']) >= \n        assert 'VISHWAKARMA' in result['target_departments'] or 'LAKSHMI' in result['target_departments']\n\n\nclass TestCHITRAGUPTA:\n    \"\"\"Test CHITRAGUPTA recorder\"\"\"\n    \n    @pytest.mark.asyncio\n    async def test_record_decision(self, db):\n        \"\"\"Test recording CEO decision\"\"\"\n        agent = CHITRAGUPTA(db)\n        result = await agent.process(\n            \"Approve the marketing budget increase\",\n            {\"command_id\": \"test-89\", \"ceo_id\": \"ceo-\"}\n        )\n        \n        assert result['success'] == True\n        assert result['agent'] == 'CHITRAGUPTA'\n        assert result['type'] == 'decision'\n        assert 'decision_id' in result\n    \n    @pytest.mark.asyncio\n    async def test_record_query(self, db):\n        \"\"\"Test recording regular query\"\"\"\n        agent = CHITRAGUPTA(db)\n        result = await agent.process(\n            \"What's the revenue today?\",\n            {\"command_id\": \"test-\", \"ceo_id\": \"ceo-\"}\n        )\n        \n        assert result['success'] == True\n        assert result['type'] == 'query'\n\n\nclass TestAGASTYA:\n    \"\"\"Test AGASTYA learning system\"\"\"\n    \n    @pytest.mark.asyncio\n    async def test_pattern_analysis_insufficient_data(self, db):\n        \"\"\"Test pattern analysis with insufficient data\"\"\"\n        agent = AGASTYA(db)\n        result = await agent.process(\"Analyze patterns\", {})\n        \n        assert result['success'] == True\n        assert result['agent'] == 'AGASTYA'\n        assert 'Not enough data' in result['message']\n    \n    @pytest.mark.asyncio\n    async def test_pattern_analysis_with_data(self, db):\n        \"\"\"Test pattern analysis with sufficient data\"\"\"\n        # Insert mock command history\n        commands = [\n            {\"raw_command\": \"Check URJAA status\", \"timestamp\": \"--T::\"},\n            {\"raw_command\": \"Show revenue\", \"timestamp\": \"--T::\"},\n            {\"raw_command\": \"URJAA charging stations\", \"timestamp\": \"--T::\"},\n        ] * 4  # Duplicate to get  commands\n        \n        await db.ceo_commands.insert_many(commands)\n        \n        agent = AGASTYA(db)\n        result = await agent.process(\"Analyze my patterns\", {})\n        \n        assert result['success'] == True\n        assert len(result['insights']) >  or len(result['recommendations']) > \n\n\nclass TestGaneshaOrchestrator:\n    \"\"\"Test GANESHA sub-agent orchestration\"\"\"\n    \n    @pytest.mark.asyncio\n    async def test_orchestrator_knowledge_query(self, db):\n        \"\"\"Test orchestration of knowledge query\"\"\"\n        orchestrator = GaneshaSubAgentOrchestrator(db)\n        result = await orchestrator.process_command(\n            \"What is URJAA?\",\n            {\"command_id\": \"test-orch-\", \"ceo_id\": \"ceo-\"}\n        )\n        \n        assert 'CHITRAGUPTA' in result['sub_agents_invoked']  # Always invoked\n        assert 'SARASWATI' in result['sub_agents_invoked']  # Knowledge query\n        assert len(result['outputs']) >= \n    \n    @pytest.mark.asyncio\n    async def test_orchestrator_directive(self, db):\n        \"\"\"Test orchestration of directive\"\"\"\n        orchestrator = GaneshaSubAgentOrchestrator(db)\n        result = await orchestrator.process_command(\n            \"Tell finance team to review budget\",\n            {\"command_id\": \"test-orch-\", \"ceo_id\": \"ceo-\", \"priority\": \"P\"}\n        )\n        \n        assert 'CHITRAGUPTA' in result['sub_agents_invoked']\n        assert 'NARADA' in result['sub_agents_invoked']  # Communication\n        assert result['outputs']['NARADA']['success'] == True\n\n\nclass TestCommandProcessor:\n    \"\"\"Test command processing and intent classification\"\"\"\n    \n    @pytest.mark.asyncio\n    async def test_intent_classification_query(self):\n        \"\"\"Test QUERY intent classification\"\"\"\n        processor = CommandProcessor()\n        result = await processor.process(\"What is our revenue?\", use_ai=False)\n        \n        assert result['intent'] == 'QUERY'\n        assert 'revenue' in str(result['entities']).lower() or result['intent'] == 'QUERY'\n    \n    @pytest.mark.asyncio\n    async def test_intent_classification_directive(self):\n        \"\"\"Test DIRECTIVE intent classification\"\"\"\n        processor = CommandProcessor()\n        result = await processor.process(\"uild a new feature\", use_ai=False)\n        \n        assert result['intent'] == 'DIRECTIVE'\n    \n    @pytest.mark.asyncio\n    async def test_entity_extraction_department(self):\n        \"\"\"Test department entity extraction\"\"\"\n        processor = CommandProcessor()\n        result = await processor.process(\"inance team, review the budget\", use_ai=False)\n        \n        assert 'department' in result['entities']\n        assert 'LAKSHMI' in result['entities']['department']\n    \n    @pytest.mark.asyncio\n    async def test_priority_assignment(self):\n        \"\"\"Test priority assignment\"\"\"\n        processor = CommandProcessor()\n        \n        # Critical priority\n        result = await processor.process(\"URGENT: System is down\", use_ai=False)\n        assert result['priority'] == 'P'\n        \n        # Normal priority\n        result = await processor.process(\"Show me the dashboard\", use_ai=False)\n        assert result['priority'] in ['P', 'P3']\n\n\nclass TestConversationContext:\n    \"\"\"Test conversation context management\"\"\"\n    \n    @pytest.mark.asyncio\n    async def test_context_creation(self, db):\n        \"\"\"Test creating new conversation context\"\"\"\n        manager = ConversationContextManager(db)\n        processor = CommandProcessor()\n        \n        processed = await processor.process(\"What's URJAA revenue?\", use_ai=False)\n        context_id = await manager.update_context(\"ceo-\", \"What's URJAA revenue?\", processed)\n        \n        assert context_id is not None\n        \n        context = await manager.get_context(\"ceo-\")\n        assert context['context_id'] == context_id\n        assert len(context['history']) == \n    \n    @pytest.mark.asyncio\n    async def test_context_reference_resolution(self, db):\n        \"\"\"Test resolving references using context\"\"\"\n        manager = ConversationContextManager(db)\n        processor = CommandProcessor()\n        \n        # irst command\n        processed = await processor.process(\"What's URJAA revenue?\", use_ai=False)\n        await manager.update_context(\"ceo-\", \"What's URJAA revenue?\", processed)\n        \n        # Second command with reference\n        context = await manager.get_context(\"ceo-\")\n        resolved = await manager.resolve_references(\"Compare it to last month\", context)\n        \n        assert 'URJAA' in resolved or 'it' not in resolved\n\n\nclass TestTaskRouter:\n    \"\"\"Test task routing and creation\"\"\"\n    \n    @pytest.mark.asyncio\n    async def test_task_creation(self, db):\n        \"\"\"Test creating a task\"\"\"\n        router = TaskRouter(db)\n        task_id = await router.create_task(\n            command_id=\"test-cmd-\",\n            command=\"Review budget\",\n            department=\"LAKSHMI\",\n            priority=\"P\",\n            entities={\"department\": [\"LAKSHMI\"]}\n        )\n        \n        assert task_id is not None\n        \n        task = await router.get_task_status(task_id)\n        assert task is not None\n        assert task['status'] == 'queued'\n        assert task['department'] == 'LAKSHMI'\n    \n    @pytest.mark.asyncio\n    async def test_route_command(self, db):\n        \"\"\"Test routing a complete command\"\"\"\n        router = TaskRouter(db)\n        processor = CommandProcessor()\n        \n        processed = await processor.process(\"inance, review Q4 budget\", use_ai=False)\n        result = await router.route_command(\n            command_id=\"test-route-\",\n            command=\"inance, review Q4 budget\",\n            processed=processed,\n            ceo_id=\"ceo-\"\n        )\n        \n        assert result['success'] == True\n        assert len(result['tasks_created']) > \n        assert 'LAKSHMI' in result['departments']\n    \n    @pytest.mark.asyncio\n    async def test_department_workload(self, db):\n        \"\"\"Test getting department workload\"\"\"\n        router = TaskRouter(db)\n        \n        # Create a task first\n        await router.create_task(\n            command_id=\"test-workload-\",\n            command=\"Test task\",\n            department=\"LAKSHMI\",\n            priority=\"P\",\n            entities={}\n        )\n        \n        workload = await router.get_department_workload(\"LAKSHMI\")\n        \n        assert 'active_tasks' in workload\n        assert 'completed_today' in workload\n        assert workload['department'] == 'LAKSHMI'\n\n\nclass TestEndToEnd:\n    \"\"\"End-to-end integration tests\"\"\"\n    \n    @pytest.mark.asyncio\n    async def test_full_command_flow(self, db):\n        \"\"\"Test complete flow from CEO command to task creation\"\"\"\n        # . Process command\n        processor = CommandProcessor()\n        processed = await processor.process(\n            \"inance team, analyze Q4 revenue and compare to Q3\",\n            use_ai=False\n        )\n        \n        assert processed['intent'] in ['DIRECTIVE', 'ANALYSIS']\n        assert 'LAKSHMI' in processed.get('departments_involved', [])\n        \n        # . Route to departments\n        router = TaskRouter(db)\n        routed = await router.route_command(\n            command_id=\"ee-test-\",\n            command=\"inance team, analyze Q4 revenue\",\n            processed=processed,\n            ceo_id=\"ceo-\"\n        )\n        \n        assert routed['success'] == True\n        assert len(routed['tasks_created']) > \n        \n        # 3. Invoke sub-agents\n        orchestrator = GaneshaSubAgentOrchestrator(db)\n        sub_results = await orchestrator.process_command(\n            \"inance team, analyze Q4 revenue\",\n            {\"command_id\": \"ee-test-\", \"ceo_id\": \"ceo-\"}\n        )\n        \n        assert 'CHITRAGUPTA' in sub_results['sub_agents_invoked']\n        assert 'NARADA' in sub_results['sub_agents_invoked']\n        \n        # 4. Verify task exists\n        task_id = routed['tasks_created'][]['task_id']\n        task = await router.get_task_status(task_id)\n        \n        assert task is not None\n        assert task['status'] == 'queued'\n    \n    @pytest.mark.asyncio\n    async def test_multi_turn_conversation(self, db):\n        \"\"\"Test conversation context across multiple turns\"\"\"\n        processor = CommandProcessor()\n        context_manager = ConversationContextManager(db)\n        \n        # Turn \n        processed = await processor.process(\"What's URJAA revenue this month?\", use_ai=False)\n        await context_manager.update_context(\"ceo-\", \"What's URJAA revenue this month?\", processed)\n        \n        # Turn  with reference\n        context = await context_manager.get_context(\"ceo-\")\n        command = await context_manager.resolve_references(\"Compare it to last month\", context)\n        processed = await processor.process(command, use_ai=False)\n        await context_manager.update_context(\"ceo-\", command, processed)\n        \n        # Verify context maintained\n        final_context = await context_manager.get_context(\"ceo-\")\n        assert len(final_context['history']) == \n        assert 'URJAA' in str(final_context['entities']) or final_context['topic'] is not None\n\n\n# Run tests\nif __name__ == \"__main__\":\n    pytest.main([__file__, \"-v\", \"-s\"])\n