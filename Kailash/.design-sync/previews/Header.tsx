import { Header } from 'frontend';

// The marketing header for the public site; takes no props and reads router
// context, which DesignSystemProvider supplies.
export const Default = () => <Header />;
