import { LegalFooter } from 'frontend';

// Carries the legal links; renders react-router <Link>s, so it needs the router
// context DesignSystemProvider provides.
export const Default = () => <LegalFooter />;
