"""Verification checks for production readiness.

Each module here is runnable as `python -m scripts.verify.<name>` and shares the
contract in `common.py`: one exit-code enum, one Finding shape, one renderer.
"""
