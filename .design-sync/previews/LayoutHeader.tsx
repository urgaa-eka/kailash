import { LayoutHeader } from 'frontend';

// The app-shell header (src/components/Layout/Header.js), renamed to keep the
// plain `Header` name for the marketing one.
export const Default = () => <LayoutHeader onMenuToggle={() => {}} />;
